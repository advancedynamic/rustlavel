# The production profile the benchmark runs under. It exists to be explicit
# about what is *not* on: no devtools, no live reload, no DEBUG logging, no SQL
# echo. Everything it sets is what an ordinary deployment would set.
spring.thymeleaf.cache=true
server.error.include-stacktrace=never
server.error.include-message=never
