[workspace]
resolver = "2"
members = ["crates/*"]

[workspace.package]
version = "0.5.3"
edition = "2024"
# Measured, not assumed. Edition 2024 puts the floor at 1.85, but the code uses
# let-chains (`&& let`) in twenty-one places, and those stabilised in 1.88.
#
# The first version of this line said 1.85 — reasoned from the edition and from
# a grep that looked for nested `if let` rather than for `&& let`, so it missed
# every one of them. Installing the toolchain and building is what found it:
# 1.87 refuses with ``let` expressions in this position are unstable`, 1.88
# builds the whole workspace with every feature and every test target.
#
# A declared floor that is too low is worse than none: cargo tells a consumer
# on 1.86 that the crate is compatible, and rustc then refuses it.
#
# **Declaring it here is half the job.** `[workspace.package]` is opt-in: a
# member inherits a field only by asking for it, so every crate carries
# `rust-version.workspace = true`. Without that line in all thirty-three, this
# one reaches no published manifest at all — crates.io reported `rust_version:
# null` for 0.5.2 and a consumer on 1.87 still got `let expressions in this
# position are unstable` from inside `rustlavel-core`. With it, cargo says
# `rustc 1.87.0 is not supported ... requires rustc 1.88` and stops.
rust-version = "1.88"
license = "MIT"
authors = ["Advance Dynamic Software"]
repository = "https://github.com/advancedynamic/rustlavel"

[workspace.dependencies]
rustlavel-core = { path = "crates/rustlavel-core", version = "0.5.3" }
rustlavel-http = { path = "crates/rustlavel-http", version = "0.5.3" }
rustlavel-auth = { path = "crates/rustlavel-auth", version = "0.5.3" }
rustlavel-debugbar = { path = "crates/rustlavel-debugbar", version = "0.5.3" }
rustlavel-i18n = { path = "crates/rustlavel-i18n", version = "0.5.3" }
rustlavel-client = { path = "crates/rustlavel-client", version = "0.5.3" }
rustlavel-cache = { path = "crates/rustlavel-cache", version = "0.5.3" }
rustlavel-db = { path = "crates/rustlavel-db", version = "0.5.3" }
rustlavel-ldap = { path = "crates/rustlavel-ldap", version = "0.5.3" }
rustlavel-otel = { path = "crates/rustlavel-otel", version = "0.5.3" }
rustlavel-search = { path = "crates/rustlavel-search", version = "0.5.3" }
rustlavel-macros = { path = "crates/rustlavel-macros", version = "0.5.3" }
rustlavel-view = { path = "crates/rustlavel-view", version = "0.5.3" }
rustlavel-model-cache = { path = "crates/rustlavel-model-cache", version = "0.5.3" }
rustlavel-metrics = { path = "crates/rustlavel-metrics", version = "0.5.3" }
rustlavel-oauth = { path = "crates/rustlavel-oauth", version = "0.5.3" }
rustlavel-oauth-provider = { path = "crates/rustlavel-oauth-provider", version = "0.5.3" }
rustlavel-openapi = { path = "crates/rustlavel-openapi", version = "0.5.3" }
rustlavel-storage = { path = "crates/rustlavel-storage", version = "0.5.3" }
rustlavel-validation = { path = "crates/rustlavel-validation", version = "0.5.3" }
rustlavel-vault = { path = "crates/rustlavel-vault", version = "0.5.3" }
rustlavel-telescope = { path = "crates/rustlavel-telescope", version = "0.5.3" }
rustlavel-ai = { path = "crates/rustlavel-ai", version = "0.5.3" }
rustlavel-audit = { path = "crates/rustlavel-audit", version = "0.5.3" }
rustlavel-flags = { path = "crates/rustlavel-flags", version = "0.5.3" }
rustlavel-rbac = { path = "crates/rustlavel-rbac", version = "0.5.3" }
rustlavel-queue = { path = "crates/rustlavel-queue", version = "0.5.3" }
rustlavel-mail = { path = "crates/rustlavel-mail", version = "0.5.3" }
rustlavel-mcp = { path = "crates/rustlavel-mcp", version = "0.5.3" }
rustlavel-webauthn = { path = "crates/rustlavel-webauthn", version = "0.5.3" }
rustlavel-ws = { path = "crates/rustlavel-ws", version = "0.5.3" }
tokio = { version = "1", features = ["full"] }
