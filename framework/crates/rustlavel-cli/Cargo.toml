[package]
name = "rustlavel-cli"
description = "The rustlavel command-line tool: new, serve, make:*"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
readme = "README.md"
authors.workspace = true
repository.workspace = true

[[bin]]
name = "rustlavel"
path = "src/main.rs"

[dependencies]

[dev-dependencies]
# Tests only. The published binary still depends on nothing: this is here so a
# guard can read the kit's language files with the project's own JSON parser
# rather than a hand-rolled one.
rustlavel-core.workspace = true
